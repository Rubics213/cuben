name: Vulnerability Scanning & Subdomain Detection

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '13 1 * * *'
    - cron: '47 5 * * *'
    - cron: '29 9 * * *'
    - cron: '53 14 * * *'
    - cron: '11 18 * * *'

jobs:
  vulnerability-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ... (your installation and subdomain detection steps unchanged) ...

      - name: Notify Slack - New Subdomains Detected
        if: env.new_subdomains_detected == 'true'
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "text": "🌐 *New subdomains were detected for Shopify.com.*\nStarting automated scans..."
            }' \
            "${{ secrets.Slack_Hook }}"

      - name: Run OWASP ZAP Scan
        if: env.new_subdomains_detected == 'true'
        uses: zaproxy/action-baseline@v0.10.0
        with:
          target: 'https://shopify.com'
          rules_file_name: '.github/zap/rules.tsv'

      - name: Upload ZAP report
        if: env.new_subdomains_detected == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: owasp-zap-report.html

      - name: Notify Slack - ZAP Complete
        if: env.new_subdomains_detected == 'true'
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "text": "🕷️ *OWASP ZAP scan completed* for Shopify.com.\nReport: zap-report.html"
            }' \
            "${{ secrets.Slack_Hook }}"

      - name: Run Nikto Scan
        if: env.new_subdomains_detected == 'true'
        run: |
          mkdir -p nikto-output
          nikto -h https://shopify.com -output nikto-output/niktoreport.txt

      - name: Upload Nikto report
        if: env.new_subdomains_detected == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: nikto-report
          path: nikto-output/niktoreport.txt

      - name: Notify Slack - Nikto Complete
        if: env.new_subdomains_detected == 'true'
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "text": "🔍 *Nikto scan completed* for Shopify.com.\nReport: niktoreport.txt"
            }' \
            "${{ secrets.Slack_Hook }}"

      - name: Notify Slack - Scan Summary
        if: env.new_subdomains_detected == 'true'
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "text": "✅ *All scans complete.* Reports have been uploaded.\nWorkflow: '${{ github.workflow }}'\nBranch: '${{ github.ref }}'\nCommit: <https://github.com/${{ github.repository }}/commit/${{ github.sha }}|${{ github.sha }}>"
            }' \
            "${{ secrets.Slack_Hook }}"

      - name: Display Scan Summary
        run: echo "OWASP ZAP and Nikto Scans Completed"
